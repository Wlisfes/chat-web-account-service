apiVersion: v1
kind: ConfigMap
metadata:
  name: chat-web-account-service-config
data:
  PORT: "3000"
  NODE_ENV: "production"
  NACOS_SERVER: "host.docker.internal:8848"
  NACOS_NAMESPACE: "0210cecf-114e-43e6-a260-1d4b263fb189"
  NACOS_GROUP: "DEFAULT_GROUP"
  NACOS_SERVICE_NAME: "chat-web-account-service"
  NACOS_CONFIG_DATA_ID: "chat-web-account-service.yaml"
  NACOS_CONFIG_GROUP: "DEFAULT_GROUP"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-web-account-service
  labels:
    app: chat-web-account-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-web-account-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: chat-web-account-service
    spec:
      containers:
        - name: app
          image: chat-web-account-service:latest
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - configMapRef:
                name: chat-web-account-service-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: chat-web-account-service
  labels:
    app: chat-web-account-service
spec:
  type: LoadBalancer
  selector:
    app: chat-web-account-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
